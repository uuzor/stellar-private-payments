import { XdrPrimitiveType } from './xdr-type.js';
import { XdrWriterError } from './errors.js';

class Float extends XdrPrimitiveType {
  /**
   * @inheritDoc
   */
  static read(reader) {
    return reader.readFloatBE();
  }

  /**
   * @inheritDoc
   */
  static write(value, writer) {
    if (typeof value !== 'number') throw new XdrWriterError('not a number');

    writer.writeFloatBE(value);
  }

  /**
   * @inheritDoc
   */
  static isValid(value) {
    return typeof value === 'number';
  }
}

export { Float };
//# sourceMappingURL=float.js.map
