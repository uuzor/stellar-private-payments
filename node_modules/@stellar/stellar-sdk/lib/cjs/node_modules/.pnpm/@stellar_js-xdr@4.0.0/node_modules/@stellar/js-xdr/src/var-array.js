'use strict';

var unsignedInt = require('./unsigned-int.js');
var xdrType = require('./xdr-type.js');
var errors = require('./errors.js');

class VarArray extends xdrType.NestedXdrType {
  constructor(
    childType,
    maxLength = unsignedInt.UnsignedInt.MAX_VALUE,
    maxDepth = xdrType.NestedXdrType.DEFAULT_MAX_DEPTH
  ) {
    super(maxDepth);
    this._childType = childType;
    this._maxLength = maxLength;
  }

  /**
   * @inheritDoc
   */
  read(reader, remainingDepth = this._maxDepth) {
    xdrType.NestedXdrType.checkDepth(remainingDepth);
    const length = unsignedInt.UnsignedInt.read(reader);
    if (length > this._maxLength)
      throw new errors.XdrReaderError(
        `saw ${length} length VarArray, max allowed is ${this._maxLength}`
      );

    // Upper-bound fast-fail: remaining bytes is a loose capacity check since
    // each XDR element typically consumes more than 1 byte (e.g., 4+ bytes)
    if (length > reader.remainingBytes()) {
      throw new errors.XdrReaderError(
        `VarArray length ${length} exceeds remaining ${reader.remainingBytes()} bytes`
      );
    }

    const result = [];
    for (let i = 0; i < length; i++) {
      result.push(this._childType.read(reader, remainingDepth - 1));
    }
    return result;
  }

  /**
   * @inheritDoc
   */
  write(value, writer) {
    if (!(value instanceof Array))
      throw new errors.XdrWriterError(`value is not array`);

    if (value.length > this._maxLength)
      throw new errors.XdrWriterError(
        `got array of size ${value.length}, max allowed is ${this._maxLength}`
      );

    unsignedInt.UnsignedInt.write(value.length, writer);
    for (const child of value) {
      this._childType.write(child, writer);
    }
  }

  /**
   * @inheritDoc
   */
  isValid(value) {
    if (!(value instanceof Array) || value.length > this._maxLength) {
      return false;
    }
    for (const child of value) {
      if (!this._childType.isValid(child)) return false;
    }
    return true;
  }
}

exports.VarArray = VarArray;
//# sourceMappingURL=var-array.js.map
